import 'package:flutter/material.dart';

class changeRoute extends StatelessWidget{
  const changeRoute ({super.key});

  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text("Change My Route")
      ),
    );
  }    
}